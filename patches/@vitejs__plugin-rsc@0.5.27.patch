diff --git a/dist/plugin-BK29Va7z.js b/dist/plugin-BK29Va7z.js
index 548c631..b34b86e 100644
--- a/dist/plugin-BK29Va7z.js
+++ b/dist/plugin-BK29Va7z.js
@@ -408,0 +409 @@ function vitePluginResolvedIdProxy() {
+
@@ -409,0 +411 @@ function scanBuildStripPlugin({ manager }) {
+	const lexedModules = /* @__PURE__ */ new Map();
@@ -413,0 +416,6 @@ function scanBuildStripPlugin({ manager }) {
+		buildStart() {
+			if (manager.isScanBuild && manager.scanBuildObservers.size > 0) {
+				lexedModules.clear();
+				for (const observer of manager.scanBuildObservers) observer({ type: "reset", environmentName: this.environment.name });
+			}
+		},
@@ -416 +424 @@ function scanBuildStripPlugin({ manager }) {
-			async handler(code, _id, _options) {
+			async handler(code, id, _options) {
@@ -419 +427 @@ function scanBuildStripPlugin({ manager }) {
-					code: await transformScanBuildStrip(code),
+					code: await transformScanBuildStrip(code, manager.scanBuildObservers.size > 0 ? (imports, exports) => lexedModules.set(id, { code, imports, exports }) : void 0),
@@ -422,0 +431,7 @@ function scanBuildStripPlugin({ manager }) {
+		},
+		moduleParsed(info) {
+			if (!manager.isScanBuild || manager.scanBuildObservers.size === 0) return;
+			const lexed = lexedModules.get(info.id);
+			if (!lexed) return;
+			lexedModules.delete(info.id);
+			for (const observer of manager.scanBuildObservers) observer({ type: "module", environmentName: this.environment.name, info, ...lexed });
@@ -427,2 +442,3 @@ const importGlobRE = /\bimport\.meta\.glob(?:<\w+>)?\s*\(/g;
-async function transformScanBuildStrip(code) {
-	const [imports] = esModuleLexer.parse(code);
+async function transformScanBuildStrip(code, onLexed) {
+	const [imports, exports] = esModuleLexer.parse(code);
+	onLexed?.(imports, exports);
@@ -546,0 +563 @@ var RscPluginManager = class {
+	scanBuildObservers = /* @__PURE__ */ new Set();
diff --git a/dist/plugin-Bm-Fb4g7.d.ts b/dist/plugin-Bm-Fb4g7.d.ts
index b0fdd93..3ddad9e 100644
--- a/dist/plugin-Bm-Fb4g7.d.ts
+++ b/dist/plugin-Bm-Fb4g7.d.ts
@@ -3 +3,2 @@ import MagicString from "magic-string";
-import { Plugin, ResolvedConfig, Rollup, ViteDevServer, parseAstAsync } from "vite";
+import { ImportSpecifier, ExportSpecifier } from "es-module-lexer";
+import { ModuleInfo, Plugin, ResolvedConfig, Rollup, ViteDevServer, parseAstAsync } from "vite";
@@ -27,0 +29,11 @@ type ServerRerferenceMeta = {
+type ScanBuildObserver = (event: {
+  type: "reset";
+  environmentName: string;
+} | {
+  type: "module";
+  environmentName: string;
+  code: string;
+  imports: readonly ImportSpecifier[];
+  exports: readonly ExportSpecifier[];
+  info: ModuleInfo;
+}) => void;
@@ -39,0 +52 @@ declare class RscPluginManager {
+  scanBuildObservers: Set<ScanBuildObserver>;
@@ -185 +198 @@ declare function transformRscCssExport(options: {
-export { ResolvedAssetsManifest as a, getPluginApi as c, vitePluginRscMinimal as d, ResolvedAssetDeps as i, transformRscCssExport as l, AssetsManifest as n, RscPluginManager as o, PluginApi as r, RscPluginOptions as s, AssetDeps as t, vitePluginRsc as u };
\ No newline at end of file
+export { ResolvedAssetsManifest as a, getPluginApi as c, vitePluginRscMinimal as d, ResolvedAssetDeps as i, transformRscCssExport as l, AssetsManifest as n, RscPluginManager as o, PluginApi as r, RscPluginOptions as s, AssetDeps as t, vitePluginRsc as u };
